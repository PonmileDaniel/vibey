{
    "build": {
      "command": "cd client && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/",
      "directory": "backend"
    },
    "start": {
      "command": "cd backend && npm install && NODE_ENV=production npm start",
      "directory": "backend",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }